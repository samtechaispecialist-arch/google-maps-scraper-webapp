{
  "name": "google-maps-scraper-webapp",
  "version": "1.0.0",
  "description": "Full-stack web app for Google Maps scraping with rotating proxies and MCP integration",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "build": "npm run build --workspace=client && npm run build --workspace=server",
    "start": "npm start --workspace=server",
    "test": "npm test --workspaces"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
